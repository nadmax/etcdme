# Claude Code UI resources kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Note: namespace is created by root-app (sync-wave -1) before secrets
  # Note: route is managed by root-app overlay for domain customization
  - deployment.yaml
  - service.yaml
  - pvc.yaml
